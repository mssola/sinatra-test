= Leaky

This is a Sinatra test application. If you want to run it locally,
just perform the following command:

  $ rackup config.ru

This site is using PostgreSQL. Don't forget to set your password in a
bash environment variable.
